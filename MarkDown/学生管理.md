# 学生管理系统

### 概要设计
1. 前端
```
	root-学校1-学院1     
	   |    |-学院2   
	   |-学校2-学院1 
```
2. 数据结构  
树   
```
遍历树(Tree)
增加节点(Tree,Node)
删除节点(Tree,Node)

```
树的节点  
```c
	Struct Node
   {
		//指向父节点的指针
		int code;//代码
		char name[100]//名称;
		//指向子节点的指针
   }
```
堆栈节点
```
   Struct SNode
   {
	   code char[6];
	   //指针 ;
   }
```
   堆栈

1. 模块功能   
   * ##### 层级 
	root-学校-学院-专业-班级-学生    
   	编码 1-1-1-1-01    
   * ##### 查询 关键字(fd)
   	输入编码或者名称 输出当前树的所有子树    
   	学生需要详细信息 单独制表
   * ##### 增加 修改 删除 关键字(ad cg de)
	通过接收关键字以及后面的编码 来实现


4. 函数设计
	##### 初始化
	* void Init(Tree *a,Stack a)
	##### 接收用户输入
	* void Catch(char S[100])      
	抓取用户输入的中的关键字和待操作的编码对象
	```
	char tmp[2];
	char tmpcode[6];	 
	if(tmp[2]==ad){Ad(Tree *a,tmpcode)}
	else.....
	//格式错误特判重输入
	```
	##### 增加 
	* void Ad(Tree *a,char code[6])
	

	##### 删除
	* void De(Tree *a,char code[6])
	##### 修改
	* void Cg(Tree *a,char code[6])
	##### 查 
	* void Fd(Tree *a,char a[100],int Mode)
	```
	//Mode 0精确 Mode 1模糊
	if(!mode)
	{
		char tmpcode[6];
		//只要a[100]前面的六个code字符
		遍历树(){
			Node.code==tmpcode[6]{
			//后续输出;
			}
		}
	}
	else
	{
		遍历树(){
			if(Node.name==a[100]){
				后续输出;
			}
		}
	}
	``` 
5. Ex
   ##### 撤销